# ─── Frontend ───
# In development, Vite proxy handles /api and /ws — no need to set these.
# For production builds (docker), the nginx proxy does the same.

VITE_API_URL=http://localhost:4000
VITE_WS_URL=ws://localhost:4000
